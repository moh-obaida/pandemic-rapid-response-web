{
  "rules": {
    "rooms": {
      "$roomCode": {
        ".read": "auth != null",
        ".write": "auth != null && (!data.exists() || data.child('status').val() === 'lobby' || auth.uid === data.child('hostId').val() || newData.hasChild('pendingAction'))",
        ".validate": "newData.hasChildren(['status', 'lobbyPlayers', 'settings', 'hostId']) && newData.child('status').isString()"
      },
      ".indexOn": ["status", "createdAt"]
    },
    ".read": false,
    ".write": false
  }
}
